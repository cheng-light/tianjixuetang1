server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tianji_academy?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 136888
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

app:
  jwt:
    secret: "tianji-academy-jwt-secret-change-this-in-production-2026"
    expiration-minutes: 720
  upload-dir: uploads
